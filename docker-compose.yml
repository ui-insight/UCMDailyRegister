services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ucm
      POSTGRES_PASSWORD: ucm_dev
      POSTGRES_DB: ucm_newsletter
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
